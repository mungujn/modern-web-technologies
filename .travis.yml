language: node_js
node_js:
  - "stable"
cache: npm
script:
  - npm install
  - npm test
  - npm run build
deploy:
  provider: pages
  github_token: $githubToken
  local_dir: build
  on:
    branch: master